 @model IEnumerable<TesteMVC.Pessoa>
@{
    ViewData["Title"] = "Pessoas";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>



@foreach (var item in Model) {
    <tr>
        <td>Nome: @Html.DisplayFor(modelItem => item.Nome)</td>
        <td>SobreNome: @Html.DisplayFor(modelItem => item.Sobrenome)</td>
        <td>Cidade: @Html.DisplayFor(modelItem => item.Cidade)</td>
    </tr><br>
}
